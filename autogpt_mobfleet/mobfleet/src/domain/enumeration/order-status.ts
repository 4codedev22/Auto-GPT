/**
 * The OrderStatus enumeration.
 * Reference to Status: https=//docs.pagar.me/reference/pedidos-1
 */
export enum OrderStatus {
      PENDING = 'pending',
      PAID = 'paid',
      CANCELED = 'canceled',
      FAILED = 'failed',
}